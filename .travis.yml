language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t arisahirata/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true arisahirata/docker-react npm run test
